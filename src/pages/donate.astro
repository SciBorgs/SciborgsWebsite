---
import { Icon } from "astro-icon/components";

const showEmail = false;
---

<div class="container">
  <img class="tiers" src="/sponsorTiers.png" />
  <p>
    Lorem ipsum dolor sit amet consectetur adipiscing elit. Quisque faucibus ex
    sapien vitae pellentesque sem placerat. In id cursus mi pretium tellus duis
    convallis. Tempus leo eu aenean sed diam urna tempor. Pulvinar vivamus
    fringilla lacus nec metus bibendum egestas. Iaculis massa nisl malesuada
    lacinia integer nunc posuere. Ut hendrerit semper vel class aptent taciti
    sociosqu. Ad litora torquent per conubia nostra inceptos himenaeos.
  </p>
  <p>Donate At:</p>
  <div class="emailContainer">
    <div id="toggleEmails">
      <Icon id="selector" name="mdi:menu-right" />
      <p>Toggle to show emails</p>
    </div>
    <div id="emailsContainer">
      <h2 class="header">SciBorgs Contact</h2>
      <h2 class="header">Sponsorships Lead</h2>
      <p>(quicker response time)</p>
    </div>
  </div>
</div>

<style>
  .container {
  }

  .tiers {
    width: 100%;
    height: auto;
    filter: drop-shadow(3px 3px 5px rgb(0 0 0));
    border: 1px black solid;
    border-radius: 0.25rem;
  }

  .emailContainer {
    display: flex;
    flex-direction: column;
    width: 100%;
  }

  #toggleEmails {
    display: flex;
    align-items: center;
    padding: 0.5rem;
    border: 1px black solid;
    width: 100%;
    height: 100%;
  }

  #selector {
    transition: rotate 150ms ease-in-out;
    rotate: 0deg;
  }

  #selector.activeIcon {
    rotate: 90deg;
  }

  #emailsContainer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 150ms ease-in-out;
  }

  #emailsContainer.emailExpand {
    max-height: 500px;
    overflow: visible;
  }

  .header {
    margin-block: 0;
  }
</style>

<script is:inline>
  let showEmail = false;

  const toggle = document.getElementById("toggleEmails");
  const container = document.getElementById("emailsContainer");
  const icon = document.getElementById("selector");

  toggle.addEventListener("click", () => {
    showEmail = !showEmail;

    container.classList.toggle("emailExpand", showEmail);
    icon.classList.toggle("activeIcon", showEmail);
  });
</script>
